'use server';

// Auth is fully managed by Clerk.
// Sign-in: handled by <SignIn /> component (components/login-form.tsx)
// Sign-out: handled by <UserButton /> component (components/logout-button.tsx)
// Session tokens: obtained via auth().getToken() (server) or useAuth().getToken() (client)
//
// This file is intentionally empty. Do not add custom login/logout actions here.
export {};

